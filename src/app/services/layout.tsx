import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Services | Web Development, SEO, Google Ads - Dubai Freelancer",
  description:
    "Website development, SEO, Google Ads, social media marketing, landing pages, WordPress, Shopify. Full digital services from a Dubai-based freelancer.",
};

export default function ServicesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
